{
  "buildCommand": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
